// import { useLoaderData } from 'react-router-dom';

// interface User {
//     name: string;
//     email: string;
// }

const UserProfile = () => {
  //   const user: User = useLoaderData();

  return (
    <div>
      <h1>User Profile</h1>
    </div>
  );
};

export default UserProfile;
